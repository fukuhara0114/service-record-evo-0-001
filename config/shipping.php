<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 1日あたりの出荷予定台数上限（目安）
    |--------------------------------------------------------------------------
    |
    | shippingOut_requiredDate の日別件数の目安。超過しても登録は可能だが、
    | カレンダー上で赤背景などにより超過を明示する。
    | .env の SHIPPING_DAILY_CAPACITY で変更可能。
    |
    */
    'daily_capacity' => (int) env('SHIPPING_DAILY_CAPACITY', 8),

];
